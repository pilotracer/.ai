# Local development stack - proposal

**Status:** Draft · **Created:** YYYY-MM-DD  
**Requires:** owner approval before creating `docker-compose.yml`, `Dockerfile.*`, `.env.example` (`.cursorrules` §Protected Files)

---

## 1. Goals

- Isolated compose project per clone (`COMPOSE_PROJECT_NAME`)
- Services: REPLACE:SERVICE_LIST
- Host ports via `.env` (`*_HOST_PORT`)

---

## 2. Service table

| Service key | Image / build | Purpose | Host port var |
|---------------|---------------|---------|---------------|
| REPLACE:SERVICE_API | | API | REPLACE:API_PORT_VAR |
| REPLACE:SERVICE_DB | | Database | |

---

## 3. Volumes and networks

| Name | Mount | Notes |
|------|-------|-------|
| | | |

---

## 4. Dev entrypoint

- Script: `REPLACE:DEV_STACK_SCRIPT`
- Health checks: REPLACE:HEALTH_ENDPOINTS

---

## 5. Approval

| Role | Approved | Date |
|------|----------|------|
| Owner | pending | |

After approval: create protected files; record in HANDOFF; run `@dev-stack` if script missing.
