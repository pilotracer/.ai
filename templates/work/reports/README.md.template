# Audit reports (`.work/reports/`)

Optional, **non-binding** snapshots from verify passes. Canonical registry remains `{FEATURE_SPEC_ROOT}/<slug>/*-SPEC.md` and DIRECTORY_MAP.

| File | Produced by |
|------|-------------|
| `YYYYMMDD-code-registry-audit.md` | `@plan-verify coverage` (when user asks to persist) |

Do not duplicate full path inventories here if they already live in SPEC **Implementation map** sections.
